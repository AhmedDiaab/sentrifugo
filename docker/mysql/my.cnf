[mysqld]
# Safer installs when SQL defines functions/triggers
log_bin_trust_function_creators=ON

# Good defaults
character-set-server=utf8mb4
collation-server=utf8mb4_unicode_ci

# Optional: lower sql_mode strictness if legacy SQL chokes
# sql_mode=
